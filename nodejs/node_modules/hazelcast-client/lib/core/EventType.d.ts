export declare enum EventType {
    ADDED = 1,
    REMOVED = 2,
    UPDATED = 4,
    EVICTED = 8,
    EVICT_ALL = 16,
    CLEAR_ALL = 32,
    MERGED = 64,
    EXPIRED = 128,
    INVALIDATION = 256,
}
